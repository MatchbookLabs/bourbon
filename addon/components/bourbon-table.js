import Component from '@ember/component';
import layout from '../templates/components/bourbon-table';

export default Component.extend({
  classNames: ['BourbonTable'],
  tagName: 'table',
  layout
});
